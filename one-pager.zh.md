# 当模型开始思考，评测框架在静默地失效

**一句话**：推理模型把输出额度花在「思考」上 → 正文为空或只剩思考 → 框架**报成功、给 0 分、一声不响**。
我用**三家框架 × 两个模型 × 两套数据集**的同题受控对照把它钉住，并把四条上游缺陷和全部可复现材料公开。

---

## 四个读数（都能自己复跑）

| # | 读数 | 说明 |
|---|---|---|
| 1 | **0.0000 / 0.0000 / 0.7315** | 同一批题、同一个模型，三家框架自报的数 |
| 2 | **0.0000 ↔ 0.9333** | **同框架 · 同任务 · 同提示 · 同一个「16」的额度，只换端点** ⇒ 病根是**(额度字段 × 端点 × 服务端是否把推理计入)** 这个三元组，不是「哪家框架差」 |
| 3 | **两家供应商同一形状** | 16 / 256 token ⇒ 崩；4096 / 512 ⇒ 好（第二家把思考**内联写在正文里**：正文不空，但没有可判读的答案） |
| 4 | **信号其实都在** | 每一个归零格子，API 早给了 `finish_reason ∈ {length, max_tokens}` ⇒ **不是无法知道，是知道了没说** |

## 我做了什么

- **对照矩阵**：三家独立框架 × GSM8K 300 题 / MMLU 300 题 × 两个模型，逐题对齐；
- **三层归因**（判分链路 / 推理链路 / 实现参数），**两张表并存**（预注册阶梯 + 动因归层），分类器逐字节可复算；
- **三把尺**：判分桥自检（64/64 腿复现率 1.000）· 六条预期带（数值越界即 `exit 1`）· **人评 20 处预注册盲评**（四条预测全部成立，其中一条**推翻了我自己的判据**）；
- **自己抓出三处装置错**并重算全部读数 —— 最严重那处（假 L1 占 **46.3%**）会让我把结论讲反；
- **四条上游缺陷报告**已提交，每条带自包含最小复现。

## 公开凭证

- 可复现仓库：**https://github.com/laa1991/silent-zero**（含**无依赖的判定器**与不花 API 的框架复现脚本）
- 四条报告：inspect_evals **#2473** · inspect_ai **#5467** · lm-eval **#4187** · open-compass **#2647**

## 30 秒口述版

> 我在三家主流评测框架上跑同一个模型的同一批题，发现它们能报出 **0.0000 / 0.0000 / 0.7315**。
> 其中两个零**长得一模一样但机理完全不同**：一个正文是空的，一个正文写了但判分器读不出来。
> 我把「模型先思考」这件事隔离出来：**同一个任务、同一个提示、同一个 16 token 的额度，只换一个端点，
> 就从 0.0000 变成 0.9333** —— 所以问题不在模型、也不在某一家框架，
> 在**额度、端点与判分读法这三处实现约定，都还假设模型开口就答**。
> 现在这条链路上**每一个环节都留下了可复跑的凭据**，四条缺陷也报给了上游。

## 边界（不吹的地方）

**不排名**（没有全量人工标注，就不说哪家更准）· **不报流行率**（n=24–30/格）· **不报显著**（没算 CI）。
