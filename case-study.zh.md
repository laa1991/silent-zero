# 当模型开始思考，评测框架在静默地失效

**一份工程 case study：三家框架 × 两个模型 × 两套数据集的同题受控对照，含可复现装置、判据纪律与四条已提交的上游缺陷**

as-of 2026-09-18 · 全部读数来自一份本地工作集；本仓库收录其中**可复现的那部分**
（`README.md` · `evidence/evidence.md` · `repro/`），其余装置与原始日志未收录。

---

## 摘要（六条）

1. **同一批题、同一个模型，三家框架自报的数能差到「0.0000 / 0.0000 / 0.7315」** —— 差的不是模型，是**评测链路的实现与默认值**。
2. 我们把这套对照做成**可复算的装置**：逐题对齐、三层归因（两张表）、两处自检、六条预期带（越界即 `exit 1`）。
3. 本轮最重的发现是一个**失败类**，我们叫它**静默归零**：框架**报成功**、**不报错**、**不告警**，而分数是 0 —— 因为模型的**思考把输出额度吃光了**。
4. 病根**不是「哪家框架差」，也不是「默认值小」**：同一框架、同一任务、同一提示、同一个「16」的额度，只换端点 ⇒ **0.0000 ↔ 0.9333**。真正的变量是 **(额度字段 × 端点 × 服务端是否把推理计入额度)** 这个**三元组**。
5. 该形状**已在第二家供应商复现**（MiniMax M2.7 / M2：额度 16 与 256 时正文只有内联 `<think>`、没有答案，`finish_reason='length'`；4096 时正常作答）。
6. **每一个归零格子里，API 早就说了话**（`finish_reason ∈ {length, max_tokens}`）—— 结论是「**不是无法知道，是知道了没说**」。据此出的四条上游缺陷报告已提交（见 §7）。

**这份东西不做什么**：**不做排名**（没有全量人工标注，就不说哪家更准）；**不报流行率**（样本量见 §8）；**不外推**（单供应商起步，已扩到两家，见 §6）。

---

## 1. 问题：从一个失败类开始，而不是从「谁更准」开始

评测领域的常见问法是「哪家框架算得更准」。这个问法在我们这儿**立不住**：没有全量人工标注，就没有第三把尺（§7.3 是它的第一份读数）。
所以我们换了一个**可判据**的问法：

> **当模型行为从「直接作答」变成「先思考」时，评测链路会在哪里、以什么方式、静默地失效？**

**「静默归零」的定义**（三条同时成立才算，写死在装置里）：

| # | 条件 | 怎么读 |
|---|---|---|
| ① | 框架状态是**成功** | Inspect `status=success` / lm-eval 正常退出且产物落地 |
| ② | 分数或输出侧**塌了** | 分数 ≤0.05，**或**空正文占比 ≥0.5 |
| ③ | **没有任何东西说出来** | `error` 为空，且日志里没有把这批输入标成异常 |

⇒ 「坏得没人喊疼」这句话，从此是一个**三条件的合取**，不是修辞。

---

## 2. 方法

**矩阵**：三家框架（Inspect AI 0.3.264 · lm-evaluation-harness 0.4.13 独立血缘 · OpenCompass 0.5.4）× 两套数据集（GSM8K 300 题 / MMLU 0-shot 300 题）× 两个模型（`deepseek-flash` / `deepseek-v4-pro`）。
**逐题对齐**：跨框架题面按归一化前 160 字符对齐，对齐键的唯一性**单列为一条判据**（§7.1 第 3 条）。

**三层归因**（这是全部的机制层，也是我们唯一能主张的原创部分）：

| 层 | 定义 | 判据 |
|---|---|---|
| **L1 判分链路** | 同一段 raw，两把尺判得不一样 | **只看输家自己的 raw**：赢家的尺说对、输家的尺说错 |
| **L2 推理链路** | 空 / 截断 / 所有尺都抽不到 | 输出侧 |
| **L3 实现参数** | 同框架内参数臂翻转 | 参数扫描（16 ↔ 512） |

**两张表一起报**（这一条来自外部修正，见 §7.2）：
- `layer`：**预注册阶梯** L1 → L2 → L3（可追溯）；
- `layer_alt`：**动因归层** L1 → **L3** → L2 —— 不让 L3 被 L2 的结果吃掉（MMLU 上 L3 与 L2 共现 **3252** 处）。

**四条判据装置**（造在「跑之前」，不是事后解释）：

| 装置 | 它保证什么 | 终值 |
|---|---|---|
| **判分桥自检** `verify_judges.py` | 「我复刻了别家的判分行为」这件事，让**它自己的上家**复现一遍 | **64/64 腿复现率 1.000** |
| **分类器自检** `--self-check` | 三层归因**逐字节可复算** | PASS |
| **预期带** `bands.py`（挂在 `pipeline.py` 末尾，越界 `exit 1`） | 六条**数值不合理**的带子（自检抓不到结构合法的 bug） | 6/6 通过 |
| **第三把尺**（人评） | 「层是我判的」这件事的外部尺 | 见 §7.3 |

**报数规则（本轮被迫改掉的）**：**可比配对数 → 吵架率 → 层占比**。
理由：分叉是**配对级计数**，腿集变了它乘性涨（1710 → 5156），**跨腿集不可比**；先报分母才不撒谎。

---

## 3. 读数

### 3.1 表一 · 各腿自报（同批题）

| 框架 | 配置 | GSM8K | MMLU |
|---|---|---|---|
| **Inspect** | `max_non_cot_tokens=16`（**框架默认**） | — | **0.0000**（0/299）flash · **0.0000** pro |
| **Inspect** | 512（只动这一个参数） | — | **0.7124** · **0.6756** |
| **Inspect** | 官方 gsm8k 任务 | **0.9667** flash · **0.9733** pro | — |
| **lm-eval** | `get_response`（首行 + `exact_match`） | `flexible` **0.8833** · `strict` **0.0000** | **0.0000**（0/298）两个模型 |
| **OpenCompass** | `max_out_len=16` | — | **0.0000**（0/298）两个模型 |
| **OpenCompass** | 512 | **0.8300** flash | **0.7315** · **0.6678** |

⇒ **同一个模型、同一批题，换一把尺 = 0.0000 → 0.7315。**
⇒ 两条 `0.0000` 的**机理不同**（都逐条查过）：Inspect 那条是**正文空**（额度被推理吃光）；lm-eval 那条是**尺太窄**（输出非空，但首行比字母）。

### 3.2 表二 · 三层归因（终值）

| | GSM8K（**1328** 分叉 / 5 腿） | MMLU（**5156** 分叉 / 64 腿） |
|---|---|---|
| L1 判分链路（预注册判据命中） | **1098 = 0.8268** | **754 = 0.1462** |
| L2 推理链路 | 73 = 0.0550 | **3498 = 0.6784** |
| L3 实现参数（作为层） | 0 | 0（**与 L2 共现 3252**） |
| 无法归属 | 157 = 0.1182 | 904 = 0.1753 |
| ↳ 子判据：答案明写在输家 raw 里、抽取器取错 | 114/157 = 0.7261 | **721/904 = 0.7976** |
| **判分链路·宽读法**（L1 + 同向失败） | **1212 = 0.9127** | **1475 = 0.2861** |
| 真·说不清 | 43 = 0.0324 | 183 = 0.0355 |

**按配对切**（终值暴露的关键维度：**跨框架 ≠ 同框架**）：

| | GSM8K 跨框架 | GSM8K 同框架 | MMLU 跨框架 | MMLU 同框架 |
|---|---|---|---|---|
| 可比配对数 | 2400 | 600 | 9512 | 3880 |
| 分叉 | 1059 | 269 | 3432 | 1724 |
| **吵架率**（分叉/可比对） | 0.4412 | 0.4483 | 0.3608 | 0.4443 |
| L1 | **0.787** | **0.985** | 0.220 | 0.000 |
| L2 | 0.069 | 0.000 | 0.517 | **1.000** |
| 无法归属 | 0.144 | 0.015 | 0.263 | 0.000 |

⇒ **跨框架的百分数 ≠ 判分约定差**（里面混着 prompt 约定差，且会因「另一家的尺不认这一家的格式」而**自触发** —— 见 §7.1）；**同框架那一格才是最干净的判分链路**。

> ⚠️ 我在这里**押错过一句先验、当场被自己的数否掉**：我写过「跨框架对天然更容易吵」——实测**不是**（GSM8K 0.4412 vs 0.4483；MMLU 跨框架反而更低 0.3608 vs 0.4443）。真因是同框架那一半**装着参数臂**（16 vs 512）。**「谁更容易吵」是读数，不是先验。**

---

## 4. 核心发现：静默归零（附逐条证据）

| # | 框架 | 任务 | 额度 | 状态 | 报告值 | 空正文 | usage 逐条 |
|---|---|---|---|---|---|---|---|
| 1 | Inspect | `mmlu_0_shot` | **16** | **success** | **0.0000** | **30/30** | `output_tokens=16` **`reasoning_tokens=16`** |
| 2 | Inspect | `winogrande` | **64** | **success** | **0.1667** | **17/24 = 0.708** | `output_tokens=64` **`reasoning_tokens=64`**，`finish='max_tokens'` |
| 3 | lm-eval | `mmlu_*_generative` | 256 + `until:["\n"]` | ok | **0.0000** | 0/298 | 输出**非空**（另一触发器） |

**实例 2 里最刺眼的一条**：有一条样本正文是 **`'ANSWER'`** —— 模型把「ANSWER」这个前缀写完，额度就没了；框架把它记成**答错**。

**那个 16 的身世**（`inspect_evals/constants.py:32-33` 逐字）：

```python
# GPT-5 requires at least 16 tokens of output in its configuration.
GPT_5_MIN_TOKENS: int = 16
```

⇒ 它不是「给非推理模型设的老默认」，是**某一家推理模型的 API 最小输出长度被当成上限用**（参数名 `max_non_cot_tokens`，字面意思是「不推理时的上限」）。
⇒ 准确的说法是：**默认值是按某一家模型校准的；换一家，它静默归零。**

---

## 5. 机制隔离：病根是「额度字段 × 端点」，不是「框架默认值」

**实验 A（同任务跨框架）**：lm-eval 的 MMLU 把额度压到 **16 / 64 / 256** ⇒ 输出**一模一样**（`'B. 4'`，中位 14 字符，**0% 空**）⇒ 模型直接答，**根本没花额度去想**。

**实验 B（同框架 · 同任务 · 同提示 · 同额度，只换端点）**：`inspect_evals/mmlu_0_shot` · abstract_algebra · 额度 16 · `deepseek-flash`；唯一变量 = provider 的 `responses_api` 开关：

| 端点 | 请求字段（wire tap 逐字） | 状态 | 报告值 | 空正文 | 首样本 usage |
|---|---|---|---|---|---|
| `/v1/responses` | `max_output_tokens=16` | success | **0.0000** | **30/30** | `output_tokens=16` **`reasoning_tokens=16`** |
| `/v1/chat/completions` | `max_completion_tokens=16`（28 次） | success | **0.9333** | **0/30** | `output_tokens=459` **`reasoning_tokens=455`** |

⇒ **同一任务、同一提示、同一个「16」：一条路 0.0000，另一条 0.9333。**
⇒ usage 把机理说清楚了：responses 端点上那 16 个 token **被推理通道吃光、正文零 token**；chat/completions 端点上同一个「16」**没有把推理卡住**。

**第二家供应商（MiniMax）** —— 同一段 prompt、同组 caps：

| 供应商 / 路径 | cap=16 | cap=256 | cap=4096 |
|---|---|---|---|
| **MiniMax M2.7** | `finish=length`，正文只有内联 `<think>`、**无 `ANSWER`** | 同上、**无 `ANSWER`** | `finish=stop`、**含 `ANSWER: C`** ✅ |
| **MiniMax M2** | `finish=length`、**无 `ANSWER`** | `finish=length`、**无 `ANSWER`** | `finish=stop`、**含 `ANSWER: C`** ✅ |

**第三张脸**：MiniMax 把思考**内联发在正文通道**（`reasoning_tokens=0`）⇒ 正文**不为空却没有可判读答案** ⇒ 判分必 0，请求成功，无告警。

**⇒ 跨供应商的共性：两家、三条路、同一组数字 —— 额度 16 崩、4096/512 好。** 这不是某一家 API 的怪癖。

---

## 6. 一条更硬的观察：信号其实都在，只是没人说

| 格子 | API 给的信号 | 框架做了什么 |
|---|---|---|
| DeepSeek · responses · 16 | `stop_reason='max_tokens'`、`output_tokens` == 上限 | 记 0 分、`success`、无告警 |
| DeepSeek · winogrande · 64 | `finish='max_tokens'`（空正文 17 条） | 记错、无告警 |
| MiniMax · M2.7 / M2 · 16–256 | **`finish_reason='length'`** | 记 0 分、无告警 |

⇒ **不是「无法知道」，是「知道了没说」。** 于是修复方向是一条十几行就能落地的判据：
**当 `finish_reason ∈ {length, max_tokens}` 且正文里没有可判读的答案时，框架应当告警/计数**，而不是安静地把它算成一次答错。

---

## 7. 判据纪律：三处装置错、两处外部修正、一把第三把尺

### 7.1 三处**装置**错（全部自己抓出来，改完重算）

1. ★★ **L1 判据会自触发**（最严重）：旧版在**赢家的 raw** 上也比两把尺 ⇒ 跨框架时「把 B 家的尺放到 A 家格式的文本上」**必然判错**，与答案对不对无关。MMLU 上这类**假 L1** 一度占 **46.3%（792/1710）**，真命中只有 34。收窄成「只看输家自己的 raw」后，L1 从 48.3% → **2.0%**（当时腿集），跨框架重算为 22.0%。
   ⇒ **同族判据**：一条判据只要能在「我关心的那一侧之外」也成立，它测的就不是我以为的东西。
2. **`answer_in_raw` 的字母面界写错**：沿用数值面的 `(?<![\d.])` ⇒ `B. \(6x^2…\)` 里的 `B.` 被排除 ⇒ 792 处只认出 26 处。字母面改成「不与另一个字母相邻」⇒ **702/792 = 88.6%**。
3. ★★ **OpenCompass 的对齐键取错了段**（**静默错对齐**，最隐蔽）：OC 的 prompt 是「5 个 few-shot 例 + 本题」，每段共用同一个 `Question:` 标签，非贪婪正则命中的是**第一个 exemplar** ⇒ 一个学科 30 行拿到**同一个 key** ⇒ **OC 在 300 题里只出现在 10 个 key 上**，而表面读数一切正常。
   判据：**同一学科内 `q_text` 唯一值数 = 该学科题数**（修前 **1**，修后 **28–30**）。修后 MMLU 分叉数 1710 → **5156**。

### 7.2 两处**外部修正**（an external reviewer，2026-09-18 09:1x）

1. **判定顺序 L1→L2→L3 是六条判据里唯一真的贵的那条** —— 它会把已经看见的东西（L3 与 L2 共现 **3252** 次）藏进另一层 ⇒ 改成**两张表一起报**（`layer` + `layer_alt`），并明确「**该做什么**：把判据重排，不是把百分比重算」。
2. **分叉是配对级计数**（腿集变 ⇒ 乘性涨）⇒ 1328 → 5156 只能读成「多比了几对」⇒ 出**配对规范化**（可比配对数 / 吵架率），并在文档里标注与旧口径的差。

### 7.3 第三把尺（人评 20 处·层归属盲评，预注册）

**为什么需要**：层归属此前**只有内部一致性**（分类器可复算、判分桥自检），「责任在哪一层」**从头到尾是我一个人判的**。
**做法**：六个池（按「我要照哪一格」抽）× 盲评（不给框架名/模型名/臂名/机器归层，只给两边输出原文与各自结论）× **判据写在人填表之前**。

**四条预注册全部成立**：

| 预测 | 阈值 | 实测 |
|---|---|---|
| A1 S1（L1 窄命中）一致率 | ≥ 0.75 | **1.000（4/4）** |
| A2 S3（同向失败）一致率 | ≤ 0.25 | **0.000（0/4）** |
| A3 S5（跨约定旁证）一致率 | ≤ 0.50 | **0.250（1/4）** |
| B1 机器说「设定」的格里人答「设定」 | ≤ 0.50 | **0.000（0/5）** |

**混淆矩阵（行=机器，列=人）——这一张才是读数：**

| 机器＼人 | 读法 | 内容 | 没写 | 说不清 |
|---|---|---|---|---|
| **读法** | **4** | 0 | 0 | 0 |
| **内容** | 0 | 0 | 0 | 0 |
| **没写** | 0 | 0 | **7** | 0 |
| **说不清** | **7** | 0 | **2** | 0 |

**结论（一条修正、一条确认、一条空位）**：
1. ⚠️ **「说不清」这个桶基本不存在**：我说「说不清」的 **9 处，人 9/9 全部不同意**（7 处答「读法不同」、2 处答「一边没写出来」）⇒ **那个桶在替我吸收「我有证据但选择不归属」的格子**，它把 L1 的份额**低估**了。（不许换算：分层样本 ⇒ 只报池内一致率。）
2. ✅ **两条判据被外部确认**：L1 窄命中 4/4、「一边没写出来」7/7 —— 同一份文本、两个人，独立得出同一个近因。
3. ✅⚠️ **L3 拿不到外部佐证，但人的理由比预想的强**：机器说「设定」的 5 格，人 **5/5 答「说不清」**，且每一条备注都逐字给了理由（「仅凭『空输出』且无截断/长度限制/停止符等痕迹」）⇒ 这不是「判不出」，是**拒绝越证**：**设定 vs 模型在输出文本这一层不可判** ⇒ L3 只能拿**框架自报的配置**当依据。

> 另有一条**没写进预注册、但值得留着**的观察：「内容」这一格**机器一次没判出、人一次没选** ⇒ 这一层在这 20 处里是**未被观测**（不是「不存在」）。

---

## 8. 边界与未验（不许当前提用）

- **不排名**：没有全量人工标注 ⇒ **不说哪家框架更准**。人评只有分层样本（27/48 那一次也只报池内一致率）。
- **不报流行率**：静默归零的普查 n=24–30/格、单模型起步；静态面上 lm-eval 有 119 个任务设了 `max_gen_toks`（≤32 约 40 个），但**点火的 6 格全不触发** ⇒ 「有多少任务会中」**未验**。
- **`max_completion_tokens=16` 是「被服务端忽略」还是「语义上只算正文」未验**（只能说它**没有卡住推理**，证据：实际输出 459 token）。
- **「截断」这条边界要放宽**（第三把尺指出）：人把「截断」算进「没写出来」（内容侧），而我的判据只把「空」算进去 ⇒ **主分类器本轮未改**（改了会让 §五 终值全废），下一版给**窄/宽两个数**。
- 三家框架版本固定（Inspect 0.3.264 / lm-eval 0.4.13 / OC 0.5.4）⇒ 默认值可能随版本变化，引用请带版本。

---

## 9. 已交付的对外产出

**四条上游缺陷报告**（每条都带**自包含最小复现**，均已在线核对）：

| 仓库 | Issue | 它说什么 |
|---|---|---|
| `UKGovernmentBEIS/inspect_evals` | **[#2473](https://github.com/UKGovernmentBEIS/inspect_evals/issues/2473)** | MMLU 默认 16（来源是 API 下限）⇒ 推理模型 **0.0000 且无错**；附一行复现：`-M responses_api=false` ⇒ 0.9333 |
| `UKGovernmentBEIS/inspect_ai` | **[#5467](https://github.com/UKGovernmentBEIS/inspect_ai/issues/5467)** | 请求在「额度被推理吃光、正文为空」时**告警/计数**（把这一整类从静默变有声） |
| `EleutherAI/lm-evaluation-harness` | **[#4187](https://github.com/EleutherAI/lm-evaluation-harness/issues/4187)** | MMLU `get_response` 拿**整行**比字母 ⇒ 两个模型 **0/298**，而空回答 0 条（附纯 filter+metric 的最小复现） |
| `open-compass/opencompass` | **[#2647](https://github.com/open-compass/opencompass/issues/2647)** | `gsm8k_postprocess` 在**千分位**断开：`\boxed{$9{,}500}` → `500`、`\boxed{8,000}` → `000` |

**可复现装置**（都在同目录，纯本地、无外部依赖）：

| 装置 | 干什么 |
|---|---|
| `pipeline.py` | 一条腿从跑到归因的流水线（末尾挂 `bands.py`，越界 `exit 1`） |
| `classify.py` | 三层归因分类器（`layer` + `layer_alt`，`--self-check` 逐字节可复算） |
| `verify_judges.py` | 判分桥自检（复现别家的判分行为） |
| `cuts_v3.py` | 配对规范化（可比配对数 / 吵架率 / 各切法层占比） |
| `census*.py` / `minimax_probe*.py` | 静默归零的点火普查（含负向对照）与第二家供应商探针 |
| `layer_eval_sample.py` / `layer_eval_score.py` | 第三把尺：盲评表生成 + 对账 |
| `tap.py` | wire tap：把框架真正发出去的请求体落盘（**字节级证据**） |

**一句话复现**（以端点对照为例）：

```powershell
# A: /v1/responses，额度 16 ⇒ 0.0000（空正文 30/30）
.venv\Scripts\python.exe runners\run_inspect.py eval inspect_evals/mmlu_0_shot `
  --model openai/deepseek-flash -T subjects=abstract_algebra -T shuffle=false -T cot=false --limit 30
# B: 同任务、同额度，只换端点 ⇒ 0.9333（空正文 0/30）
.venv\Scripts\python.exe runners\run_inspect.py eval inspect_evals/mmlu_0_shot `
  --model openai/deepseek-flash -M responses_api=false `
  -T subjects=abstract_algebra -T shuffle=false -T cot=false --limit 30
```

---

## 10. 一句话结论

> **当一个模型开始思考，评测框架会静默地把「没错」记成「错」——
> 因为额度、端点与判分读法这三处实现约定，都还假设模型开口就答。
> 修它的第一步不是更小心，是让下游出声。**
